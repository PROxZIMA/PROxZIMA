name: Update streak stats

on:
  schedule:
    - cron: "0 3 * * *" # Run every day at 03:00
  push:
    paths:
      - ".github/workflows/streak-stats.yml" # Run any time this file is modified
  workflow_dispatch: # Run manually from the Actions tab

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Generate streak stats
        uses: DenverCoder1/github-readme-streak-stats@main
        with:
          options: user=${{ github.repository_owner }}&theme=dracula&dates=8b8b8b&background=0000&hide_border=true
          path: profile/streak.svg
          token: ${{ secrets.GITHUB_TOKEN }}

      # create a release with "latest" tag so that the image can be accessed via a stable URL
      - name: Create release
        uses: ncipollo/release-action@v1
        with:
          tag: latest
          omitBody: true
          omitName: true
          draft: false
          prerelease: false
          allowUpdates: true
          artifacts: profile/streak.svg

      # - name: Commit streak stats
      #   run: |
      #     git config user.name "github-actions[bot]"
      #     git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
      #     git add profile/streak.svg
      #     git commit -m "Update streak stats" || exit 0
      #     git push